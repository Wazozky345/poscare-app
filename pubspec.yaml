name: poscare
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7

  # Icon iOS style
  cupertino_icons: ^1.0.8
  
  # Firebase
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  firebase_messaging: ^16.1.0 # <--- Ganti ke versi ini

  # --- TAMBAHAN UNTUK CETAK PDF ---
  pdf: ^3.10.8      
  printing: ^5.11.1 
  fl_chart: ^0.63.0  
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1 

# KONFIGURASI ICON POSCARE
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/favicon.png"
  adaptive_icon_background: "#ffffff" 
  adaptive_icon_foreground: "assets/images/poscare_logo.png"
  min_sdk_android: 21 

flutter:
  uses-material-design: true

  # --- ASSETS ---
  assets:
    - assets/images/